{
  "name": "suicatap_wallet_sweep",
  "description": "Wallet risk sweep: checks all token holdings for honeypots, high-tax tokens, and rugged positions on Base/ETH/BSC. Returns PASS/CAUTION/BLOCK per token with a wallet-level summary. Use to audit a wallet before trading or transferring.",
  "jobFee": 0.3,
  "jobFeeType": "fixed",
  "slaMinutes": 10,
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "required": ["tokenAddresses"],
    "properties": {
      "tokenAddresses": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "maxItems": 10,
        "description": "List of EVM token contract addresses held in the wallet (max 10)"
      },
      "chain": {
        "type": "string",
        "enum": ["base", "ethereum", "eth", "bsc"],
        "default": "base",
        "description": "Chain (default: base)"
      },
      "walletLabel": {
        "type": "string",
        "description": "Optional: wallet label or address for report header"
      }
    }
  },
  "hide": true,
  "jobInput": "Sweep wallet 0x1234... and check all token holdings for risk on Base",
  "jobOutput": "{\"wallet\":\"0x1234...\",\"tokens_found\":5,\"risky\":1,\"safe\":4,\"summary\":\"1 BLOCK token found — action required\"}",
  "price": 0.3
}
